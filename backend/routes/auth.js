/**
 * Auth Routes
 * -----------
 * Note: Authentication is now handled client-side via Supabase Auth.
 * These routes are deprecated/removed to avoid confusion.
 */

const express = require("express");
const router = express.Router();

// Routes removed. Frontend uses Supabase Auth directly.

module.exports = router;
